version: "3.9"

services:
  bot:
    build: .
    container_name: telegram-ai-agent
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    ports:
      - "7860:7860"
